package com.ai.assistance.custard.data.model

/**
 * Prompt function type, used to distinguish which system prompt tag to use
 */
enum class PromptFunctionType {
    CHAT,
    VOICE,
    DESKTOP_PET
}